import React from "react";
import '../App.css' 

// navigation bar's layout
const NavBar = () => {
  return (
    <nav className = "navbar navbar-expand-sm bg-info">
        <a className = "navbar-brand" href="#">Realty Tool</a>
    </nav>
  );
}

export default NavBar;